import Ghost from './ghost.js'


class Pinky extends Ghost{ // rosa spøkelse
    constructor(ctx, x, y) {
        super(ctx, x, y);
    }

    

    finnPacman(xPacman, yPacman) {
        let xPunkt = xPacman,
            yPlunk = yPacman;

        let dist = Math.sqrt(Math.pow(xp))
        
    }

    
}

export default Blinky