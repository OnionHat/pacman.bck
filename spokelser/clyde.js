import Ghost from './ghost.js'


class Clyde extends Ghost{ // oransj spøkelse
    constructor(ctx, x, y, sprite) {
        super(ctx, x, y, sprite);
        this.sprite = sprite;
    }

    

    finnPacman(xPacman, yPacman) {
        let xPunkt = xPacman,
            yPlunk = yPacman;

        let dist = Math.sqrt(Math.pow(xp))
        
    }

    
}

export default Blinky